root_dir: ${env:VLA_ROOT}
tag: default
debug: false
data_dir: data
disp: false
shared_memory: false
task: manipulating-rope
mode: test
'n': 25
save_data: true
dataset:
  type: single
  images: true
  cache: true
  augment:
    theta_sigma: 60
record:
  save_video: false
  save_video_path: ${data_dir}/${task}-${mode}/videos/
  add_text: false
  fps: 20
  video_height: 640
  video_width: 720
